# ============================================
# Datilio Backend Environment Configuration
# ============================================
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to version control!

# ============================================
# Database Configuration
# ============================================
# Only POSTGRESQL_CONNECTION_STRING is required by the backend application
# Individual DATABASE_* variables are NOT used by the backend code
# They are only needed in root .env file for docker-compose.yml
POSTGRESQL_CONNECTION_STRING=postgresql+psycopg2://username:password@localhost:5432/datilio_db

# ============================================
# JWT Authentication
# ============================================
# Generate secure random strings for these:
# python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY=your-super-secret-jwt-key-change-this-in-production
JWT_REFRESH_SECRET_KEY=your-super-secret-refresh-key-different-from-above
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=100
REFRESH_TOKEN_EXPIRE_MINUTES=10080

# ============================================
# OpenAI API
# ============================================
OPENAI_API_KEY=sk-proj-your-openai-api-key-here

# ============================================
# Qdrant Vector Database
# ============================================
QDRANT_HOST=localhost
QDRANT_PORT=6333

# ============================================
# Email Configuration (SMTP)
# ============================================
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-specific-password
FROM_EMAIL=noreply@yourdomain.com
CONTACT_EMAIL=contact@yourdomain.com
FRONTEND_URL=http://localhost:5173

# ============================================
# Stripe Payment Processing
# ============================================
# Test keys (use sk_live_* and pk_live_* in production)
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key

# ============================================
# CORS Configuration
# ============================================
# Comma-separated list of allowed origins
BACKEND_CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://127.0.0.1:3000,http://127.0.0.1:5173

# ============================================
# Application Settings
# ============================================
# Enable IndexedDB storage for offline support
IndexedDB_STORE_FOR_SPEED_AND_OFFLINE=False

# Environment (development, staging, production)
ENVIRONMENT=development

# ============================================
# Optional: Redis (for production rate limiting)
# ============================================
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=your_redis_password
# REDIS_DB=0

# ============================================
# Optional: Logging
# ============================================
# LOG_LEVEL=INFO
# LOG_FILE=app_logs.txt

# ============================================
# Donation Configuration
# ============================================
# Buy Me a Coffee donation link
BUYMEACOFFEE_URL=https://buymeacoffee.com/datilio

# ============================================
# Security Notes
# ============================================
# 1. Never commit .env to git
# 2. Use strong, unique keys for production
# 3. Rotate JWT secrets periodically
# 4. Use environment-specific values
# 5. Keep this file backed up securely

